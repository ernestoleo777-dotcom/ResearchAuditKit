# Gate Semantics

Gate results are `PASS`, `INCONCLUSIVE`, `FAIL`, `BLOCKED`, `SKIPPED_BY_GATE`, or `UNADJUDICATED`. Criteria are evaluated exactly as configured. Missing metrics do not become failures or passes; they remain unadjudicated. A pass is local to the registered criteria and never means that a paper, theory, or engineering decision is endorsed.

