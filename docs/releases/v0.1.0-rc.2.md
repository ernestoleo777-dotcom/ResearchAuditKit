# ResearchAuditKit v0.1.0-rc.2

## Overview

RC2 is an experimental release candidate for an offline repository-integrity and
evidence-accounting toolkit. GitHub prereleases are its distribution channel.
There is no stable release and no PyPI or TestPyPI distribution.

## RC1 to RC2 changes

- Add `prediction-seal` and `prediction-verify` for local consistency records over
  opaque declarations.
- Add `isolation-audit` for declared workspace structure, overlap, path, and
  escaping-symlink checks.
- Add `evidence-index` for deterministic role-labelled evidence inventories.
- Reject duplicate keys and unsafe portable references in the new closed JSON
  contracts.
- Add machine-readable outputs and documented exit semantics for the four commands.
- Add a tested three-minute synthetic demo, command/use-case/limitation/architecture
  documentation, and minimal issue/pull-request templates.
- Include curated public documentation and the synthetic demo in the sdist while
  keeping the wheel runtime-focused.
- Declare hermetic build-test dependencies and retain the Python 3.10-3.12 Ubuntu
  CI matrix.
- Narrow public positioning to mechanical repository checks with explicit
  scientific, publication, and project-decision nonclaims.

The detailed classification is in [RC1 to RC2 Delta](RC1_TO_RC2_DELTA.md).

## Installation

Install from a reviewed source checkout:

```bash
python -m pip install .
rak --version
```

Or install the verified wheel from the GitHub prerelease:

```bash
python -m pip install "https://github.com/ernestoleo777-dotcom/ResearchAuditKit/releases/download/v0.1.0-rc.2/research_audit_kit-0.1.0rc2-py3-none-any.whl"
```

The GitHub prerelease is the distribution channel. No PyPI or TestPyPI
publication is claimed.

## Quick Start

Use the single maintained [three-minute Quick Start](../quickstart.md). Its source
installation, help/version check, PASS inventory, intentional exit-2 inventory,
and stable summary projections are executed by the automated productization test.

## Compatibility

- Python 3.10 / Ubuntu: tagged-release-commit CI pass.
- Python 3.11 / Ubuntu: tagged-release-commit CI pass.
- Python 3.12 / Ubuntu: tagged-release-commit CI pass.
- Python 3.13 / macOS arm64: local candidate acceptance evidence.
- macOS arm64: local build, installation, CLI, and demo evidence.
- Windows: unverified. The Quick Start uses POSIX shell syntax, and Windows path,
  activation, symlink, and permission behavior has not received full acceptance.

The tagged release commit passed the repository's Ubuntu CI matrix on Python
3.10, 3.11, and 3.12 before the annotated tag and prerelease were created. The
release asset manifest binds the exact commit and CI run identifiers.

## Public CLI compatibility

| Area | RC1 to RC2 result |
| --- | --- |
| Added commands | `prediction-seal`, `prediction-verify`, `isolation-audit`, `evidence-index` |
| Changed shared commands | None found in parser/handler diff for the ten RC1 commands |
| Removed or renamed commands | None |
| New output schemas | Seal JSON, isolation audit JSON, evidence index JSON/CSV, and summaries for the new commands |
| Existing output/schema changes | None found for RC1 commands |
| New overwrite behavior | `prediction-seal` refuses replacement unless `--force` |
| Existing overwrite changes | None found |
| New exit behavior | Verification/isolation findings return 2; valid evidence indexing returns 0 |
| Existing exit-code changes | None found |

No intentional public CLI breaking change was found for the ten commands shared
with RC1. The version output changes to `0.1.0rc2`, and the API remains experimental.

## Limitations

- Mechanical verification only; supplied policies and metadata can be incomplete.
- No scientific-validity, causal-validity, claim-truth, novelty, or correctness
  judgment.
- No project scoring, publication prediction, acceptance estimate, or continue/stop
  decision.
- No general reproducibility guarantee.
- Local hashes and seals do not establish authorship or an external timestamp.
- Filesystem case, symlink, permission, special-file, and platform behavior can
  affect results; Windows is unverified.
- No external adoption, effectiveness, or performance evidence is claimed.

## Security and privacy

Runtime commands are local/offline and package source does not import network
clients. The toolkit does not execute audited repository code. Portable-reference
checks reject absolute and escaping paths where those closed contracts apply, but
not every user-supplied string is a sanitized publication field. Reports can expose
filenames, paths, sizes, hashes, timestamps, and supplied metadata. Users must
review policies, inputs, output directories, permissions, and reports before
sharing them.

## Artifact integrity

The GitHub prerelease includes `SHA256SUMS` and
`RC2_RELEASE_MANIFEST.json`. The manifest binds the tag target, source tree, CI
evidence, build environment, artifact names, sizes, and SHA-256 values. Verify the
downloaded assets with the published checksum file. The checksums apply to those
release assets only; no cross-build byte-reproducibility claim is made.

## Upgrade notes

Existing RC1 commands require no documented argument migration. New commands are
opt-in. Consumers that assert the CLI version must update the expected value from
`0.1.0rc1` to `0.1.0rc2`. Consumers adopting the new JSON contracts should account
for their closed-field, duplicate-key, identifier, and portable-reference checks.
