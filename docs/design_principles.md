# Design Principles

Evidence precedes narrative: record governed inputs, policies, metrics, and status transitions before writing conclusions. Hard gates execute before downstream claims. Warnings remain visible but do not masquerade as failures. Scientific sources are addressed by portable relative paths and protected through explicit baselines. Negative results remain first-class records. Outputs produced outside authorization are isolated as unadjudicated rather than deleted or promoted.

