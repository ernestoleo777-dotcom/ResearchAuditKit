# Protocol Deviations

Deviation records are append-only. Each record identifies its trigger, violated protocol rule, affected files and metrics, isolation status, adjudication status, and usage boundaries. Outputs are preserved but cannot enter formal summaries until explicitly adjudicated under the applicable protocol.

