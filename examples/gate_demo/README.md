# Gate Demo

Run from the repository root:

```bash
rak gate --metrics examples/gate_demo/metrics.json --policy examples/gate_demo/policy.yaml --out /tmp/rak-gate-demo
```

The configured metric falls in the `INCONCLUSIVE` band. That status is a protocol outcome, not a scientific endorsement.

