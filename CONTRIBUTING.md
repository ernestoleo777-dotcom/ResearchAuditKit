# Contributing

Use Python 3.10+, create a local virtual environment, install `.[dev]`, and run `pytest` before proposing changes. New behavior requires synthetic tests and machine-readable output. Never commit user data, absolute user paths, serialized models, generated baselines, or domain-specific claims. Changes to status semantics must document backward compatibility and add gate tests.

