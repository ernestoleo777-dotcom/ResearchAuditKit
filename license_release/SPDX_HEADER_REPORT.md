# SPDX Header Report

All 43 maintained Python files under `src/research_audit_kit/` were scanned. A single `# SPDX-License-Identifier: Apache-2.0` header was added to each; no duplicate or conflicting header was found. Test code, fixtures, configurations, and generated artifacts were intentionally excluded from the source-header policy. The module comments preserve Python module docstrings and runtime behavior.

`COPYRIGHT_HOLDER_NOT_EXPLICITLY_DECLARED`; no copyright holder was invented. A second header scan found the process idempotent. See `checks/source_header_scan.csv`.
